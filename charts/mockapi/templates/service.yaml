apiVersion: v1
kind: Service
metadata:
  name: {{ include "mockapi.fullname" . }}
  namespace: {{ include "mockapi.namespace" . }}
  labels:
    {{- include "mockapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mockapi.selectorLabels" . | nindent 4 }}
